# VLAN IP of THIS chain machine. The subtensor RPC/WS ports (9944/9933) are bound to it so the
# chain is reachable only on the private Linode VLAN, never on the public interface.
CHAIN_BIND_IP=10.0.0.10
